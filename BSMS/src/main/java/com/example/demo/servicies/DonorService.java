package com.example.demo.servicies;
/*
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

import com.example.demo.entities.BloodBank;
import com.example.demo.entities.Donor;
//import com.example.demo.entities.Request;
import com.example.demo.repositories.BloodBankRepositry;
import com.example.demo.repositories.DonorRepositry;
//import com.example.demo.repositories.RequestRepositry;

@Service
@Transactional
public class DonorService {

	
	 /*@Autowired
	    private DonorRepositry donorRepository;
	    
	    @Autowired
	    private DonorRepositry repodonor;
	    
	    public List<Donor> findAll(){
			return repodonor.findAll();
		}*/
	/*@Autowired
    private DonorRepositry donorRepository;

    @Autowired
    private BloodBankRepositry bloodBankRepository;*/

    // Other existing methods...

    /*public List<BloodBank> getBloodBanksForDonor(int donorId) {
        // Fetch the donor by ID
        Donor donor = donorRepository.findById(donorId).orElse(null);
        if (donor != null) {
            // Fetch and return blood banks by the donor's city ID
            return bloodBankRepository.getBloodBankByCity(donor.getCity());
        } else {
            return null; // or throw an exception if donor is not found
        }
    }*/
	/*
	@Autowired
    private RequestRepositry requestRepo;

    public Request saveBloodRequest(Request request) {
        return requestRepo.save(request);
    }
}*/
