package com.example.demo.repositories;

//import java.util.List;
/*
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import com.example.demo.entities.BloodBank;
import com.example.demo.entities.Donor;

@Repository
public interface DonorRepositry extends JpaRepository<Donor, Integer> {

}*/
	
/*@Query(value = "SELECT * FROM donor WHERE cityid = :cityid", nativeQuery = true)
	List<Donor> getBloodBankByCity(@Param("cityid") int cityid);*/
	//List<Donor> findByCityId(int cityId);

